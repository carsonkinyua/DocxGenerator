//Automatically generated by rifgen
use crate::*;
use jni_sys::*;
foreign_class!(
	# [doc = " A simple android doc builder"]
	class AndroidDocBuilder {
		self_type AndroidDocBuilder;
		constructor AndroidDocBuilder::new()->AndroidDocBuilder;
		# [doc = " Add a text to the doc"]
		fn AndroidDocBuilder::add_text(& mut self , text : & str); alias addText;
		# [doc = " Add Image to the doc"]
		fn AndroidDocBuilder::add_image(& mut self , file : & str , width : u32 , height : u32); alias addImage;
		# [doc = " Export the file"]
		fn AndroidDocBuilder::generate_docx(& mut self , file_name : & str); alias generateDocx;
	}
);
foreign_class!(
	class RustLog {
		fn RustLog::initialise_logging(); alias initialiseLogging;
	}
);
